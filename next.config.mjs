/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['static.wixstatic.com', 'cdn.beacons.ai', 'images.unsplash.com'],
    },
  };
  
  export default nextConfig;
  